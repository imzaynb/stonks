import { Inter, Mate_SC } from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});

export const mate_sc = Mate_SC({
    subsets: ['latin'],
    display: 'swap',
    weight: '400'
});